# 2026 校内选拔赛 B 题分析报告

## 1. 任务链与证据边界

本题不是四个相互独立的问题，而是“配准基准—含噪融合—真实数据检验—固定轨迹
调度”的递进链。Q1 在无噪声环境中建立时间偏差符号和 10 Hz 输出基准；Q2 新增
固定空间偏差与随机噪声；Q3 要求在真实数据中先检验系统偏差，再输出供 Q4 使用的
位置、速度和加速度状态；Q4 不改变机器人轨迹，只在给定轨迹上筛选连续可行窗口并
安排最多 9 条正式结果。

题面能够识别的是两定位方式之间的相对时间偏差和相对空间偏差。在没有外部真值的
Q2、Q3 中，不能把相对偏差归因于某一台设备，也不能声称融合轨迹等于绝对真值。

## 2. 输入审计摘要

| 文件 | 主要内容 | 数据规模/结构 | 用途 |
|---|---|---|---|
| 附件1.xlsx | 4 Hz、5 Hz 无噪声轨迹 | 3000、4001 行 | Q1 |
| 附件2.xlsx | 含噪、含固定偏差轨迹 | 3000、3948 行 | Q2 |
| 附件3.xlsx | 实际测量轨迹 | 1381、1621 行 | Q3、Q4 |
| 附件4.xlsx | 射击与拍照目标 | 各 18 个目标 | Q4 |
| result.xlsx | 正式答案模板 | 9 行结果槽位 | Q4交付 |

附件 1–3 的时间字段均严格递增，无缺失和重复时间戳，采样间隔与 4 Hz、5 Hz
题设一致。附件 4 的射击、拍照目标编号均唯一。`result.xlsx` 的 H–L 列包含红色
说明与填写范例，正式写入只能修改 A–E 列第 2–10 行。

## 3. 逐问数学任务

### Q1

- 推断目标：固定时间校正量 $\Delta t$。
- 输出：$\Delta t$、10 Hz 的 $(t,x,y)$ 轨迹、对齐验证。
- 主模型：异频运动特征互相关生成候选，位置 MSE 全局扫描与连续优化精估，合并
  观测后用 Cubic Spline 重建。
- 关键反证：轨迹近似周期，最高互相关峰可能是错误周期，故相关不能直接作为答案。

### Q2

- 推断目标：$\Delta t$ 与相对固定偏差 $(b_x,b_y)$。
- 状态目标：在随机噪声下恢复 10 Hz 位置轨迹。
- 主模型：Huber 剖面最小二乘把三参数问题降为一维时间搜索；偏差校正后直接在
  原始异步事件时刻运行常加速度 Kalman Filter，并用 RTS 离线平滑。
- 关键反证：先把两源都插值到 10 Hz 再送入 KF 会把插值点当成独立测量，造成
  协方差过度收缩。

### Q3

- 推断目标：相对系统偏差是否显著且具有工程意义。
- 主检验：二维 HAC-Wald；稳健对照：moving-block bootstrap；固定性诊断：偏差
  对时间的 HAC 趋势检验。
- 融合选择：检验和固定性支持偏差时采用偏差增广异步 CA-KF，否则采用普通异步
  CA-KF；两者均经 RTS 平滑输出 10 Hz 状态。
- 结论边界：$p\ge0.05$ 只能表述为“未发现显著偏差”，不能证明偏差不存在。

### Q4

- 决策目标：在固定轨迹和 9 行模板容量内尽可能多地完成任务。
- 硬约束：射击/拍照的距离、速度、加速度和连续准备时间；同一拍照目标的方向角
  差至少 $60^\circ$；任务占用区间不冲突；每个射击目标至多一次。
- 主模型：在 10 Hz 状态上识别连续可行窗口，生成代表候选后建立 0-1 MILP。
  词典序先最大化任务数，再最大化工程裕度与角度多样性。
- 验证：逐任务重放准备区间的全部状态点，与最早可行贪心基线和不确定性扰动比较。

## 4. 依赖关系

```text
Q1 时间符号、配准与输出契约
        ↓
Q2 时间/空间偏差联合辨识与含噪融合
        ↓
Q3 真实数据偏差检验 + 10 Hz位置/速度/加速度
        ↓
Q4 连续可行窗口 + MILP调度 + result.xlsx
```

Q4 使用的是 Q3 的平滑状态及不确定性，而不是对附件 3 坐标再次独立差分。这样可
避免上游时间偏差、系统偏差和测量噪声在后问中被忽略。

## 5. 实现与验证策略

数据求解和数据图统一使用 Python 3.12、NumPy/SciPy/Pandas/Matplotlib；原因是
仓库已有完整可解释资产、SciPy 提供 HiGHS MILP、环境不依赖许可证。MATLAB
R2024b 仅用于不依赖数据的流程图和参考示意图。

每问至少包含：结构等价合成测试或精确小实例、主模型与基线对照、主要指标和互补
指标、一个可能推翻结论的敏感性/失败情景。全部权威结果保存于 `05_results/`，
正式图保存于 `06_figures/`，论文段落只能引用这些产物。
