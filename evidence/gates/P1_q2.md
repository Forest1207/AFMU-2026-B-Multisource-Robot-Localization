# P1 门禁：问题二

日期：2026-08-19  
最终状态：PASS

## 独立复核与返工

独立只读复核首先指出：旧实现并非标准 Huber 损失、过程噪声调参混入门控反馈、创新相关未白化，以及创新均值/趋势诊断和离群点合成试验不足。现已完成全部返工并经同一复核者再次只读检查，最终判为 `PASS`。

## 方法一致性

- [x] 时间映射统一为 `t2_corrected=t2-time_offset`，空间相对偏差定义为 `bias=stream2-stream1`。
- [x] 固定公共评价网格上使用逐轴标准 Huber IRLS，并以真正的 Huber rho 目标剖面搜索；尺度在搜索中点估计并跨候选冻结。
- [x] 调参阶段关闭协方差膨胀，使用门控前 NIS 和逐时刻 Cholesky 白化创新的 lag-1 相关；选定过程噪声后才运行正式 99% 门控。
- [x] 正式滤波只消费原始异步事件，不把插值值作为伪观测；RTS 平滑后再输出 10 Hz 状态。

## 科学与数值验收

- [x] 正式时间偏差约 `50.17 s`，相对空间偏差约 `(3.47,-1.83) m`；敏感性情景时间偏差范围 `50.1694–50.2699 s`。
- [x] 过程噪声 `q=3.162277660168379e-8` 位于候选网格内部；门控前平均 NIS `1.9647`，白化创新平均绝对 lag-1 `0.01529`。
- [x] 分设备 HAC 创新均值检验和趋势工程效应均通过预设阈值。
- [x] 含粗大离群点的合成试验中，普通/Huber 联合参数误差为 `0.2238/0.0286`；测量方差回收、KF/RTS 协方差 PSD 和 10 Hz 输出检查均通过。
- [x] 正式 10 Hz 轨迹覆盖 `102.0–951.5 s`，共 8496 行，时间严格递增且全部有限。
- [x] 17 个敏感性情景、正式机器验证和五组 PNG/SVG/PDF 图件均通过；创新图包含 x、y 与门控前 NIS，调参图采用对数纵轴。
- [x] `parameters.json`、CSV、摘要、论文小节和图件口径一致。

## 表述边界

空间偏差仅表示两设备的相对系统差；没有外部真值时，不解释为任一设备的绝对误差。校正后两流差异和相对基线粗糙度不被表述为未知真值下的绝对定位误差。
