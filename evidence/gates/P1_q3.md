# P1 门禁：问题三

日期：2026-08-19  
最终状态：PASS

## 独立复核与返工

独立只读复核从原附件 3 临时重跑正式主链、敏感性、合成测试和图件，主要 CSV 与正式产物逐值一致。首轮发现时间平移方向文字错误、10 Hz 非事件协方差边界不清、合成零假设检验过弱、诊断冲突未显式处理以及空陈旧产物；全部修复后再次复核，最终判为 `PASS`。

## 偏差推断

- [x] `t2_corrected=t2-time_offset`；正式 `time_offset=-367.877619 s`，故方式 2 时钟落后约 367.88 s，映射时其时间戳增加 367.88 s、向后平移。
- [x] 固定公共网格上的标准逐轴 Huber 剖面配准与代码、论文一致。
- [x] HAC-Wald `p=0.3511`，工程效应指数 `0.0681<0.25`；2000 次移动块 bootstrap 两轴区间均覆盖零，HAC 趋势检验不显著。
- [x] 结论严格表述为“在当前样本与阈值下未发现具有统计与工程意义的相对系统偏差”，不表述为证明偏差严格为零。
- [x] HAC 与 bootstrap 的主判冲突会显式中止；正式数据证据一致，故不启用偏差状态。
- [x] 论文明确置信区间条件于正式时间配准值，未宣称覆盖联合配准不确定性。

## 融合与验证

- [x] 过程噪声调参关闭门控反馈，使用门控前 NIS 和 Cholesky 白化创新；正式 `q=3.1623` 位于网格内部。
- [x] 原始异步事件 KF/RTS、Joseph 更新、10 Hz 状态、速度和加速度均有限；门控前平均 NIS `2.0127`，白化 lag-1 `0.0274`。
- [x] 滤波、RTS 和输出协方差最小特征值分别为 `0.297374/0.271504/0.271504`，正式门禁检查 PSD。
- [x] 10 Hz 非事件节点被准确说明为从前一 RTS 事件状态作最大不足 0.208 s 的短时模型传播近似，不冒充严格平滑桥接。
- [x] 17 个敏感性情景的偏差判定稳定；六组正式图的 PNG/SVG/PDF 均通过视觉和结构检查。

## 合成检验

- [x] 真时间偏差 `0.18 s` 回收为 `0.1785 s`，真相对偏差 `(0.60,-0.35) m` 回收为 `(0.599,-0.355) m`。
- [x] 100 个 AR(1) 零偏差样本的经验拒绝率为 8%，预设偏差功效为 100%；30 个块 bootstrap 试验的双轴联合覆盖率为 86.7%。论文、摘要和测试输出一致。
