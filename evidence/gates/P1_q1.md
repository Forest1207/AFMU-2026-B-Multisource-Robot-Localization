# P1 门禁：问题一

日期：2026-08-19  
最终状态：PASS

## 独立复核

独立只读复核最初给出 `LIMITED`：科学计算、符号和图件全部通过，但 Windows 默认 `cp1252` 控制台会在打印希腊字母或中文路径时抛出 `UnicodeEncodeError`，使三个入口非正常退出。现已在入口显式配置 UTF-8 容错输出，并将合成恢复断言移至打印之前。

修复后在**不设置** `PYTHONIOENCODING` / `PYTHONUTF8` 的 PowerShell 环境依次运行：

1. `03_models/q1/test_synthetic.py`：退出码 0；真值 `-3.7317 s`，恢复误差 `2.199e-10 s`，RMSE `5.872e-10 m`。
2. `03_models/q1/run_q1.py`：退出码 0；正式偏差 `-198.4316999986 s`，RMSE `8.514815e-11 m`，10 Hz 输出 8495 行。
3. `03_models/q1/run_sensitivity.py`：退出码 0；8 个情景，原始偏差极差 `3.558e-09 s`。

## 科学与数值验收

- [x] 合同、代码、说明和论文统一使用 `t2_aligned = t2 + delta_t`。
- [x] 700 个精确同坐标匹配均给出 `t1-t2=-198.4317 s`，与连续估计相差约 `1.36e-9 s`。
- [x] 共同区间 700.35 s；最优点不在搜索边界。
- [x] 10 Hz 时间范围 `[221.0, 1070.4] s`，共 8495 行，严格递增、全有限、无外推。
- [x] 6301 个合并唯一节点、700 组共同采样，坐标最大差 0，最大观测间隙 0.25 s。
- [x] Linear、Cubic、PCHIP、Akima 对比与正式摘要一致。
- [x] 8 个重叠时长/粗步长/评价步长敏感性情景结论稳定。
- [x] 4 张正式图均存在 300 DPI PNG、可编辑 SVG 和矢量 PDF，人工检查无裁切或遮挡。
- [x] `parameters.json`、`summary.md`、`trajectory_10hz.csv`、论文小节和图件数字一致。

## 后续包装项

以下不阻止 Q1 P1 通过，但须在最终 GitHub 包装阶段完成：

- 明确附件放置、输入哈希校验和一键复现步骤；
- 增加精确依赖锁文件；
- 在图件清单中补充输入哈希和仓库相对来源映射。
