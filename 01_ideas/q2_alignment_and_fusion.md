# Q2 Alignment and Fusion

观测模型建议：

`z1(t_i) = r(t_i) + e1_i`

`z2(s_j) = r(s_j + Δt) + b + e2_j`

其中 `Δt` 为时间偏差，`b=(b_x,b_y)` 为固定系统偏差，`e1,e2` 为随机测量误差。

可采用外层非线性最小二乘联合估计 `Δt,b`，内层使用平滑样条/状态空间模型恢复连续轨迹；随后根据估计噪声协方差进行加权融合。
